<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <title>Document</title>
  
</head>
<body class="d-flex justify-content-center align-items-center flex-column  ">
    <form class="col-4 bg-black rounded text-center p-4 mt-5 shadow-lg   m-auto " method="post" action="/postToTodo">
          <label for="name" class="form-label text-light">Item</label>
        <input type="text" name="title" placeholder="Item" id="name" class="form-control"><br>
        <label for="text"  class="form-label text-light">Quantity</label>
        <input type="tet" name="description" placeholder=" Quantity"id="email" class="form-control">
        <br>
        <button class="btn btn-success ">Enter</button>
    </form>
    <br><br>

    <div class="m-auto">
        <h1>Show Todo List</h1>
        <% if (arr.length > 0) { %>
         <% for( let index = 0; index < arr.length; index++ ) { %>
             <% let cur = arr[index] %>
             <div class="card my-3">
                <div class="card-header">
                    <h3><%= cur.title %></h3>
                </div>
                <div class="card-body">
                    <p><%= cur.description %></p>
                </div>
                <div class="card-footer d-flex justify-content-around ">
                   <form action="/deletepost" method="post">
                    <input type="hidden" value="<%= cur._id %>" name="id">
                    <button class="btn btn-danger">delete</button>
                   </form>   
                 <a class=" btn btn-warning text-decoration-none "  href="/todo/<%= cur._id%>">edit</a>  
                </div>
             </div>
         <% } %>
        <% } else { %>
         
                  <h1 class="text-center">No Todos yet</h1>
            <% } %>
                
        </div>
        

            
    </div>
    <div></div>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</html>